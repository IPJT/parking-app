schema @auth(providers: [{ type: oidc, issuer: "{{ env.CLERK_ISSUER_URL }}" }], rules: [{ allow: private }]) {
  query: Query
}

type Car @model @search {
  name: String!
  brand: String!
  status: String!
  owner: String
}
